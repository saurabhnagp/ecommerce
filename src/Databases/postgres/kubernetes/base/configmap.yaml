apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: amcart-databases
  labels:
    app: postgres
data:
  # Optional: custom postgresql.conf snippets can be mounted here
  PGDATA: /var/lib/postgresql/data/pgdata
