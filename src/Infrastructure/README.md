# Infrastructure

| Folder | Description |
|--------|-------------|
| **nginx/** | **API gateway** image only (`amcart-gateway`). Not used to build the UI. |

The **UI** (`amcart-ui`) is built independently from **`Frontends/ui-app`** (see that project’s `Dockerfile`).
